# -*- coding: utf-8 -*-
"""Task06.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/github/Marquitos04/Curso2025-2026/blob/master/Assignment4/course_materials/notebooks/Task06.ipynb

**Task 06: Modifying RDF(s)**
"""

#!pip install rdflib
import urllib.request
url = 'https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/refs/heads/master/Assignment4/course_materials/python/validation.py'
urllib.request.urlretrieve(url, 'validation.py')
github_storage = "https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/master/Assignment4/course_materials"

"""Import RDFLib main methods"""

from rdflib import Graph, Namespace, Literal, XSD
from rdflib.namespace import RDF, RDFS
from validation import Report
g = Graph()
g.namespace_manager.bind('ns', Namespace("http://somewhere#"), override=False)
r = Report()

"""Create a new class named Researcher"""

ns = Namespace("http://mydomain.org#")
g.add((ns.Researcher, RDF.type, RDFS.Class))
for s, p, o in g:
  print(s,p,o)

"""**Task 6.0: Create new prefixes for "ontology" and "person" as shown in slide 14 of the Slidedeck 01a.RDF(s)-SPARQL shown in class.**"""

# this task is validated in the next step
ontology = Namespace("http://oeg.fi.upm.es/def/people#")
people = Namespace("http://oeg.fi.upm.es/resource/person/")
g.bind("ontology", ontology)
g.bind("person", people)

"""**TASK 6.1: Reproduce the taxonomy of classes shown in slide 34 in class (all the classes under "Vocabulario", Slidedeck: 01a.RDF(s)-SPARQL). Add labels for each of them as they are in the diagram (exactly) with no language tags. Remember adding the correct datatype (xsd:String) when appropriate**

"""

# TO DO
classes = [
    ontology.Person,
    ontology.Professor,
    ontology.AssociateProfessor,
    ontology.InterimAssociateProfessor,
    ontology.FullProfessor
]

#Declare classes
for c in classes:
    g.add((c, RDF.type, RDFS.Class))

#Labels
labels = {
    ontology.Person: "Person",
    ontology.Professor: "Professor",
    ontology.AssociateProfessor: "AssociateProfessor",
    ontology.InterimAssociateProfessor: "InterimAssociateProfessor",
    ontology.FullProfessor: "FullProfessor"
}

for cls, lbl in labels.items():
    g.add((cls, RDFS.label, Literal(lbl, datatype=XSD.string)))

#Hierarchy
g.add((ontology.Professor, RDFS.subClassOf, ontology.Person))
g.add((ontology.AssociateProfessor, RDFS.subClassOf, ontology.Professor))
g.add((ontology.InterimAssociateProfessor, RDFS.subClassOf, ontology.AssociateProfessor))
g.add((ontology.FullProfessor, RDFS.subClassOf, ontology.Professor))
# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_01(g)

"""**TASK 6.2: Add the 3 properties shown in slide 36. Add labels for each of them (exactly as they are in the slide, with no language tags), and their corresponding domains and ranges using RDFS. Remember adding the correct datatype (xsd:String) when appropriate. If a property has no range, make it a literal (string)**"""

# TO DO
props = [
    ontology.hasColleague,
    ontology.hasName,
    ontology.hasHomePage
]

for p in props:
    g.add((p, RDF.type, RDF.Property))

#Labels
g.add((ontology.hasColleague, RDFS.label, Literal("hasColleague", datatype=XSD.string)))
g.add((ontology.hasName, RDFS.label, Literal("hasName", datatype=XSD.string)))
g.add((ontology.hasHomePage, RDFS.label, Literal("hasHomePage", datatype=XSD.string)))

#Domains and ranges
g.add((ontology.hasColleague, RDFS.domain, ontology.Person))
g.add((ontology.hasColleague, RDFS.range, ontology.Person))

g.add((ontology.hasName, RDFS.domain, ontology.Person))
g.add((ontology.hasName, RDFS.range, RDFS.Literal))

g.add((ontology.hasHomePage, RDFS.domain, ontology.FullProfessor))
g.add((ontology.hasHomePage, RDFS.range, RDFS.Literal))

# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_02(g)

"""**TASK 6.3: Create the individuals shown in slide 36 under "Datos". Link them with the same relationships shown in the diagram."**"""

# TO DO
raul = people["Raul"]
asun = people["Asun"]
oscar = people["Oscar"]

#Types
g.add((raul, RDF.type, ontology.InterimAssociateProfessor))
g.add((asun, RDF.type, ontology.FullProfessor))
g.add((oscar, RDF.type, ontology.AssociateProfessor))

#Labels
g.add((raul, RDFS.label, Literal("Raul", datatype=XSD.string)))
g.add((asun, RDFS.label, Literal("Asun", datatype=XSD.string)))
g.add((oscar, RDFS.label, Literal("Oscar", datatype=XSD.string)))

#Relationships
g.add((asun, ontology.hasColleague, raul))
g.add((asun, ontology.hasHomePage, Literal("http://www.oeg-upm.net/", datatype=XSD.string)))

g.add((raul, ontology.hasColleague, asun))
g.add((raul, ontology.hasName, Literal("Raul Garcia-Castro", datatype=XSD.string)))

g.add((oscar, ontology.hasColleague, asun))
g.add((oscar, ontology.hasName, Literal("Oscar Corcho Garcia", datatype=XSD.string)))
# Visualize the results
for s, p, o in g:
  print(s,p,o)

r.validate_task_06_03(g)

"""**TASK 6.4: Add to the individual person:Oscar the email address, given and family names. Use the properties already included in example 4 to describe Jane and John (https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/master/Assignment4/course_materials/rdf/example4.rdf). Do not import the namespaces, add them manually**

"""

# TO DO
from rdflib import URIRef

foaf_email = URIRef("http://xmlns.com/foaf/0.1/email")
vcard_given = URIRef("http://www.w3.org/2001/vcard-rdf/3.0/Given")
vcard_family = URIRef("http://www.w3.org/2001/vcard-rdf/3.0/Family")

g.add((oscar, vcard_given, Literal("Óscar", datatype=XSD.string)))
g.add((oscar, vcard_family, Literal("Corcho García", datatype=XSD.string)))
g.add((oscar, foaf_email, Literal("ocorcho@fi.upm.es", datatype=XSD.string)))
# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_04(g)
r.save_report("_Task_06")